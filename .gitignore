/target
**/*.rs.bk
Cargo.lock.bak
*.profraw

# Local eval artifacts
/report.json
/report.html
# Saved runs (save-by-default; opt out with --dry-run). Dir is configurable via
# [results].dir in mira.toml.
/results/
.DS_Store

# Local Claude Code settings (per-developer)
.claude/settings.local.json
